# P4 F8 Evidence — mall-sync · import-opening · access-audit

| 项 | 值 |
| --- | --- |
| batch | F8 |
| branch | `feat/erp-f8-sync-import-audit` |
| features | `mall-sync`, `import-opening`, `access-audit` |
| workspaces | W17, W18, W19 |
| backend domains | `mall_sync`, `legacy_import`, `access_control`, `source_registry` |
| depends_on | C-G1, C-G8 |

## 1. 契约差异表

### 1.1 mall-sync (W17)

| mock / 前端字段或端点 | 后端字段 / 路径 | 处理 |
| --- | --- | --- |
| `GET` 聚合页 `fetchMallSyncPage` | 多端点并行：`/admin/mall-sales-sync-jobs`、`/admin/mall-sales-order-snapshots`、`/admin/master-mapping-tasks`、`/admin/mall-sales-reconciliation-jobs`(+items)、`/admin/mall-sales-sync-cursors`、`/admin/source-systems` | **frontend_adapt**：api.ts 组装 `MallSyncPageView` |
| `jobType` `BASELINE/INCREMENTAL/SINGLE_ORDER/RECONCILIATION` | `baseline/incremental/single_order_backfill/monthly_reconciliation` | **frontend_adapt** |
| `status` `SUCCEEDED/PARTIAL_FAILED` | `success/partial_failure` | **frontend_adapt** |
| `jobNo` 展示号 | 后端仅 `id` | **frontend_adapt**（id 截断大写）；缺专用 job_no 列登记 gap |
| `mappingStatus` `PENDING_MAPPING/DIFF/UNCHANGED` | `pending/difference/no_change` | **frontend_adapt** |
| `mappingType` `SETTLEMENT_PARTY/UNIQUE_LINE` | `settlement_entity/unique_line_item` | **frontend_adapt** |
| `differenceType` `STATUS/FINGERPRINT/DUPLICATE` | `status_difference/content_fingerprint_difference/duplicate_identity` | **frontend_adapt** |
| `ReconciliationDifference.status` `OPEN/PULLING/CONFIRMED` | `pending/backfilling/confirmed_no_difference` | **frontend_adapt** |
| `manualGovernancePolicy` | 无 HTTP 资源 | **backend_gap**（fail-closed MISSING） |
| `ownership.stage` 封存/二期 | 无 | **backend_gap**（固定第一阶段语义） |
| `history[]` 封存档案 | 无 | **backend_gap**（空数组） |
| `candidateTargets` / 映射候选 | `MasterMappingTaskView` 无候选列表 | **backend_gap** |
| `reapply` / 重新归集 | 无 reapply 端点 | **backend_gap**（mutation 返回 `BACKEND_GAP_REAPPLY`） |
| `assignMappingTask` | 无 assign 端点 | **backend_gap** |
| `triggerSingleOrderPull.externalOrderNo` | `CreateMallSalesSyncJobRequest` 无单号字段 | **backend_gap**（创建 `single_order_backfill` 作业，单号不入创建契约） |
| `fetchSourceSystems` | `GET /admin/source-systems` | **already_aligned**（status `active/disabled`→`启用/停用` 前端适配） |
| 演示 `setMallSyncDemo*` | 无 | **frontend_adapt**（no-op，保留签名） |

### 1.2 import-opening (W18)

| mock / 前端字段或端点 | 后端字段 / 路径 | 处理 |
| --- | --- | --- |
| `fetchImportBatchList` | `GET /admin/legacy-import-batches` | **frontend_adapt** |
| 前端 12 态 status（RECEIVING/SCANNING/…） | 后端 7 态 `pending_validation…failed` | **frontend_adapt**（粗映射） |
| `environment` VALIDATION/PRODUCTION | 批次无 environment 字段 | **backend_gap**（列表沿用 query 标注；详情默认 PRODUCTION） |
| `sourceObjectSet` 数组枚举 | `source_object_set` 字符串 | **frontend_adapt**（分隔解析） |
| `metrics` 四宫格 | 无聚合端点 | **frontend_adapt**（列表总数近似） |
| `confirmations[]` | `GET /admin/legacy-import-confirmations?batch_id=` | **frontend_adapt** |
| `fetchImportIssues` | `GET /admin/legacy-import-batches/{id}/rows` 过滤失败/冲突/跳过 | **frontend_adapt** |
| `issueCode` 枚举 | `error_code` 自由文本 | **frontend_adapt**（已知码映射，否则 MAPPING_CONFLICT） |
| `invalidateTrialByRuleChange` | 无 | **backend_gap** |
| `openRepairBatch` | 无 | **backend_gap** |
| `acknowledgeUploadReceived` | 无专用；读详情确认存在 | **frontend_adapt**（弱确认） |
| `initiatorLabel` / `openingPolicyHints` / 分区结果 | 后端详情无 | **backend_gap**（空/—） |

### 1.3 access-audit (W19)

| mock / 前端字段或端点 | 后端字段 / 路径 | 处理 |
| --- | --- | --- |
| roles 列表 | `GET /admin/roles` → `RoleItem` | **frontend_adapt** |
| users 列表 | `GET /admin/admins` → `AdminItem` | **frontend_adapt**（管理员账号作用户授权视图） |
| scopes 列表 | `GET /admin/data-scopes` | **frontend_adapt**（`role/user`→`ROLE/USER`） |
| fields 字段策略 | 无 field_policy 资源 | **backend_gap**（空列表 + blocker） |
| audit 列表 | `GET /admin/audit-events` | **frontend_adapt** |
| `EffectiveAccess` 聚合 | 无专用端点 | **frontend_adapt**（roles/admins/scopes/user-roles 组装） |
| `UserRoleTimePolicy` / `FieldPolicyGranularity` / `AuditAccessPolicy` | 无策略资源 | **backend_gap**（fail-closed MISSING） |
| `EMERGENCY_REVOKE_USER_ROLE` | `POST /admin/user-roles/{id}/revoke` | **frontend_adapt** |
| `ASSIGN_USER_ROLE` | `POST /admin/user-roles` | **frontend_adapt** |
| `UPDATE_FIELD_POLICY` | 无 | **backend_gap**（REJECTED） |
| 幂等查询 `resolveAccessChangeUnknown` | 无 | **backend_gap**（返回 null） |
| 演示 emptyReason 开关 | 仅前端会话 | **frontend_adapt**（no-op/session 开关，无 mock seed） |

## 2. 已删除 mock 文件

- `erp-client/mock/mall-sync.ts`
- `erp-client/mock/import-opening.ts`

未删除：

- `erp-client/mock/session-state.ts` — 仍被其它 feature（queue/fulfillment/…）引用
- access-audit 无独立 mock 文件；`session.ts` 已改为 re-export api 演示开关

## 3. 改动文件

- `erp-client/features/mall-sync/api.ts` — 真实 HTTP + 适配
- `erp-client/features/mall-sync/queries.ts` — 去掉 `isFeatureReal` 门闩
- `erp-client/features/mall-sync/mall-sync-page.tsx` — 始终挂载 SourceSystemsCard
- `erp-client/features/mall-sync/source-systems-card.tsx` — 注释更新
- `erp-client/features/import-opening/api.ts` — 真实 HTTP + 适配
- `erp-client/features/access-audit/api.ts` — 新建真实 HTTP 层
- `erp-client/features/access-audit/queries.ts` — 薄封装 api
- `erp-client/features/access-audit/session.ts` — 去掉 seed mock
- `docs/dev-plan/p4-evidence/F8.md` — 本证据

## 4. 命令与结果

```bash
# mock 引用（期望空）
grep -r "@/mock" erp-client/features/mall-sync \
  erp-client/features/import-opening \
  erp-client/features/access-audit \
  erp-client/app/\(workspace\)/governance/mall-sync \
  erp-client/app/\(workspace\)/governance/imports \
  erp-client/app/\(workspace\)/system/access-audit
# → 无输出（CLEAN）

# tsc / lint（本 worktree 内 npm ci 后）
cd erp-client && npx tsc --noEmit   # EXIT 0
cd erp-client && npm run lint       # EXIT 0（0 errors, 既有 warnings）
```

## 5. 剩余 blockers / 后端缺口（摘要）

1. 商城同步：无聚合 page、无治理策略/封存阶段/历史档案/重新归集/指派/单号入创建契约。
2. 导入：无 environment、无试算失效/修复批次、状态机细态少于前端。
3. 权限审计：无 field_policy、无 governance policy、无 effective-access 专用投影、无变更幂等查询；用户视图暂用 admins 列表。

## 6. 集成原则核对

- [x] 请求经 api.ts + TanStack Query；组件无裸 fetch
- [x] 导出函数名保持（queries 签名稳定）
- [x] 金额/数量：本批无金额运算
- [x] ApiError 透传；业务结果型 mutation 用 status/outcome，不 `throw new Error(string)`
- [x] 缺口登记，不发明后端未返回的业务事实
- [x] 未改 frozen：`lib/api/**`、`query-client.ts`、`feature-source REAL_FEATURES`、backend/**

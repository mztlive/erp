# P4 F9 Evidence — supplier-api / catalog / publications / projections

| 项 | 值 |
| --- | --- |
| batch | F9 |
| branch | `feat/erp-f9-supply-publication` |
| features | `supplier-api-connections`, `supplier-catalog`, `product-publications`, `execution-projections` |
| workspaces | W20, W21, W22, W23 |
| base | `main` |

## 1. 集成摘要

四个 feature 的 `api.ts` 已从 `@/mock/*` session-mock 改为经 `@/lib/api`（`apiGet` / `apiPost` / `apiPut`）调用真实 `/admin/...` HTTP。导出函数签名与 `queries.ts` 消费方保持稳定；分页 `Page{items,total,page,page_size}` 仅在 `api.ts` 内映射为 feature 视图类型。

- 删除本地 mock 过滤/聚合中与后端重复的部分；后端未提供的筛选/指标在 `api.ts` 内 thrift 组装并登记缺口。
- 金额/数量仅透传字符串并做展示掩码（成本字段），不做运算。
- 错误：HTTP/业务失败由 `ApiError` 传播；角色阻断等客户端前置仍可返回 FormalOutcome `blocked`。
- 不再 import `@/mock/*` 或 `mockDelay`。

## 2. 契约差异表

### 2.1 supplier-api-connections (W20) → `/admin/supplier-api-*`

| mock 字段 / 行为 | 后端字段 / 路径 | 处理 |
| --- | --- | --- |
| `connectionId` | `id` | frontend_adapt |
| `connectionCode` | `connection_code` | already_aligned (snake→camel in api) |
| `environment` DEVELOPMENT/STAGING/PRODUCTION | `production` / `testing` | frontend_adapt（DEVELOPMENT/STAGING→testing） |
| `status` ENABLED/DISABLED/FAULTED/PENDING_CONFIG | `active` / `disabled` / `fault` | frontend_adapt；**PENDING_CONFIG 无后端对应** → backend_gap |
| `lastHealth.result` SUCCESS/FAILED/… | `last_health_result` healthy/failed | frontend_adapt；AUTH_FAILED/PARTIAL/UNKNOWN 无对应 → backend_gap |
| `capabilities[].capabilityCode` CATALOG | `capability_code` product | frontend_adapt |
| `safeReferences` endpoint/credential alias | 响应永不回显 `credential_reference` | already_aligned（安全省略）；前端展示 MISSING 占位 |
| `catalog` 同步状态 / START_CATALOG_SYNC | 本域无端点 | backend_gap（目录同步在 supplier_catalog intake） |
| list `metrics` 五卡 | 无汇总端点 | backend_gap（当前页 thrift 计数，非全局） |
| `credentialOpaqueOptions` / `endpointOpaqueOptions` | 无密钥目录 API | backend_gap（空数组） |
| `relatedImpact` / `auditEvents` | 无 | backend_gap（空默认） |
| CONFIRM_CAPABILITY_REQUIREMENT | 无独立端点 | backend_gap（blocked） |
| CREATE + 仅 code/supplier/env | Create 要求 `endpoint_reference`+capabilities | frontend_adapt（占位 `pending:{idempotencyKey}`，status=disabled） |
| RUN_HEALTH_CHECK → job processing | POST `.../health-check` 同步返回 HealthCheckView | frontend_adapt（succeeded + 结果事实） |
| ENABLE/DISABLE | PUT `.../{id}` status | already_aligned |
| UPDATE_CAPABILITIES | PUT `.../{id}/capabilities` 整表替换 | frontend_adapt（读详情合并后替换） |

### 2.2 supplier-catalog (W21) → `/admin/supplier-catalog/*`

| mock 字段 / 行为 | 后端字段 / 路径 | 处理 |
| --- | --- | --- |
| 队列视图 changeType/workItem | 实体 products/skus/mappings/offerings + `/admin/work-items` | frontend_adapt：完整翻页；关系状态→changeType；异常项只绑定真实 work_item，未派发时显式阻断动作 |
| `supplierProduct.currentRevision` 丰富字段 | product+sku revision 列表 | frontend_adapt（详情拼装）；**SPU 来源修订号**用 `product.current_revision_no`，禁止误用 SKU 修订号 |
| `publicationImpact` 聚合 | `/admin/product-publications?sku_id` | frontend_adapt：按稳定公司 SKU 聚合在售/暂停数量；历史已支付订单明确显示“未聚合”，不得伪造为 0；二期安全暂停仍按 W21 验收门禁实施 |
| 池内状态 / 匹配候选 | `GET .../products/{id}/pool-match` | **already_aligned**（`MAPPED` / `HAS_CANDIDATES` / `UNMATCHED` + `match_signals`/`score`） |
| 旧 `skuCandidates` mock | 由 pool-match candidates 取代 | frontend_adapt（入池 Dialog 消费 pool-match） |
| cost mask by role | `supplier_catalog_cost:detail` 字段权限 | backend_aligned：产品详情、SKU 列表、供给列表均由 handler 服务端裁剪成本；前端不承担保密边界 |
| workItem claim/hold/complete | `/admin/work-items/{id}/*` | frontend_adapt：仅真实 work_item 可领取、暂挂、转交和完成；未派发记录不生成合成 ID |
| create / revise product | POST products / revisions | already_aligned |
| 关联入池（第二供应商/有同款） | `POST .../link-promote` | **already_aligned**（映射 Active + 双价供给；不改公司销售价） |
| 反向入池（无同款新建公司商品） | `POST .../reverse-promote` | **already_aligned**（原子 product/sku/mapping/offering） |
| 旧 promote 两步 mappings+approve | 仍可用于脚本/兼容 | frontend_adapt（主路径改 link/reverse 聚合） |
| 旧 `createCompanyProductFromSupplierSku` stub | 转发 `reversePromoteToCompanyPool` | already_aligned（deprecated 别名） |
| 入池税率 UI 整数% | 后端 `input_tax_rate` 小数串 | frontend_adapt（13 → `"0.13"`） |
| 入池不采集生效日 | 服务端业务日写修订 | already_aligned（Dialog 无 valid_from） |
| 起订量不重复录入 | 服务端读目录 SKU 修订 | already_aligned |
| fetchCompanySkuOptions | GET `/admin/skus` | frontend_adapt（关联主路径用 pool-match 候选；全库选型仍可扩展） |
| session draft | 无服务端草稿 | frontend_adapt（模块内 Map，非 mock seed） |

### 2.3 product-publications (W22) → `/admin/product-publications*`

| mock 字段 / 行为 | 后端字段 / 路径 | 处理 |
| --- | --- | --- |
| publicationStatus MALL_LIVE | `mall_effective` | frontend_adapt |
| SAFETY_PAUSED | 无 | backend_gap（映射到 PAUSED 仅手动暂停路径） |
| DeliveryStatus ACKED/HANDOFF | confirmed / manual | frontend_adapt |
| SENDING | 无 sending 状态 | backend_gap |
| SaleStatus PAUSED | `pause_order` | frontend_adapt |
| list metrics 五卡 | 无汇总 | backend_gap（当前结果 thrift） |
| fixedOffering 供应商/价 | 修订视图未嵌供给 | backend_gap（占位） |
| publishGate / safetyPause | 无 | backend_gap（READY 默认门闸；无安全暂停投影） |
| creationBlocker | 策略未确认 | backend_gap（保留阻断文案） |
| publishRevision | POST revisions + deliver | already_aligned |
| manualPause | PUT status=paused | frontend_adapt |
| resolvePublishUnknown | 无独立查询 | backend_gap（仍 unknown） |
| MALLS 选项 | GET `/admin/source-systems?system_type=MALL` | frontend_adapt |
| skuCode / productName 展示 | 列表仅 sku_id | backend_gap（暂用 id） |

### 2.4 execution-projections (W23) → `/admin/sales-order-projections*`

| mock 字段 / 行为 | 后端字段 / 路径 | 处理 |
| --- | --- | --- |
| DeliveryStatus PENDING/ACKED/ESCALATED_MANUAL | pending_send/confirmed/manual | frontend_adapt |
| UNKNOWN | 无 | backend_gap |
| ProjectionSource MIGRATION_BASELINE | cutover_snapshot | frontend_adapt |
| whitelist faceValue/cardCount/cardForm | revision 同名字段 | already_aligned |
| voucherCategory* / voucherExpiryAt | 修订视图未返回 | backend_gap（"—"） |
| salesOrderNo / customerLabel 丰富 | 仅 sales_order_id | backend_gap |
| latencyBand / reconciliation / metrics 全局 | 无 | backend_gap（默认 normal/NONE；当前页 thrift metrics） |
| as_of 时间 | `created_at` 秒戳 | frontend_adapt（不用客户端时钟作为业务 as_of；queried 用数据时间） |
| QUERY_RESULT | 读 deliveries 列表 | frontend_adapt |
| RETRY | POST `.../revisions/{no}/deliver` | already_aligned |
| ESCALATE | 无错误中心派发 | frontend_adapt（本地结果，指引 W29） |
| bulk job | 无批量端点 | frontend_adapt（串行单条 + 内存 job） |
| malls | source-systems MALL | frontend_adapt |

## 3. 已删除 mock 文件

- `erp-client/mock/supplier-api-connections.ts`
- `erp-client/mock/supplier-catalog.ts`
- `erp-client/mock/product-publications.ts`
- `erp-client/mock/product-publications-session.ts`
- `erp-client/mock/execution-projections.ts`

保留：`erp-client/mock/session-state.ts`（仍被其他 feature 引用）。

## 4. 命令与结果

```bash
# mock 引用（期望空）
grep -r "@/mock" erp-client/features/supplier-api-connections \
  erp-client/features/supplier-catalog \
  erp-client/features/product-publications \
  erp-client/features/execution-projections
# → 无输出

grep -r "@/mock" \
  erp-client/app/(workspace)/supplier-api \
  erp-client/app/(workspace)/procurement/supplier-catalog \
  erp-client/app/(workspace)/commerce/publications \
  erp-client/app/(workspace)/commerce/execution-projections
# → 无输出

cd erp-client && npx tsc --noEmit
# → exit 0（0 errors）

cd erp-client && npm run lint
# → 0 errors, 38 warnings（全仓既有 hooks/unused 警告；无 F9 阻断 error）
```


## 5. 剩余 blockers / backend gaps（优先）

1. **供应商 API 连接**：无 PENDING_CONFIG、无密钥/地址引用目录、无目录同步触发、无列表 metrics、无采购「业务能力需求确认」、健康检查同步而非后台 job 投影。
2. **供应商商品库**：changeType 已按完整目录、映射和供给关系投影，异常项已绑定真实 work_item；`publicationImpact` 仍缺服务端聚合。关联入池候选目前以条码/名称/规格弱匹配为主，全库任意公司 SKU 选型可继续增强；列表页「池内状态列」可再沉淀为常驻列（Dialog 内已展示）。
3. **商品发布**：修订/列表投影字段偏瘦（缺供给摘要、SKU 编号、完整媒体扫描态）、无安全暂停/发布门闸策略端点、无 metrics 汇总。
4. **执行投影**：白名单缺类目/有效期、无 UNKNOWN 状态、无 latency/reconciliation 服务端投影、无 bulk API、销售单号展示依赖 id。

### 5.1 W21 入池已关闭的原 gap（2026-08-07）

| 原 gap | 现状 |
| --- | --- |
| 反向创建公司商品无聚合 API | `POST /admin/supplier-catalog/reverse-promote` |
| 关联仅两步 mappings+approve 无聚合 | `POST /admin/supplier-catalog/link-promote` |
| 无池内候选匹配 | `GET /admin/supplier-catalog/products/{id}/pool-match` |
| 前端 `createCompanyProductFromSupplierSku` Validation stub | 转发 reverse-promote |

## 6. 改动文件（owns）

- `erp-client/features/supplier-api-connections/api.ts`
- `erp-client/features/supplier-catalog/api.ts`、`queries.ts`、`types.ts`
- `erp-client/features/supplier-catalog/pool-entry-dialog.tsx`（入池双分支 Dialog）
- `erp-client/features/supplier-catalog/catalog-write-dialogs.tsx`（re-export）
- 后端：`services/src/supplier_catalog/{pool_match,link_promote,reverse_promote}.rs`、handler/routes
- `erp-client/lib/permissions.generated.ts`（pool-match / link-promote / reverse-promote）
- `erp-client/features/product-publications/api.ts`
- `erp-client/features/execution-projections/api.ts`
- mock 删除见 §3
- `docs/dev-plan/p4-evidence/F9.md`（本文件）
- 业务文档：`docs/ui-workspaces/w21-supplier-catalog.md`、`docs/erp-phase-1.md`、`docs/erp-phase-2.md`、`docs/erp-mall-data-mapping.md`、`docs/ui-glossary.md`

# W24 主责迁移（已取消）

> 状态：已取消<br>
> 最后更新：2026-08-04

2026-08-04 决议取消本工作面。主责迁移是一次性运营行为而非数据实体：第二期上线时停止从商城开设新销售单，存量商城主责卡券单 `sales_order.owner_system` 由 `MALL` 改为 `ERP`，仅改字段并记录通用变更审计，不建任何专用表，无批次/冻结/基线确认流程。创建来源由 `sales_order.origin_system` 表达。来源与主责信息在 W05 销售单对象中心展示。相关同步封存逻辑见 W17。

# 列表筛选候选独立查询执行合同

版本：1.0

制定日期：2026-09-23

状态：执行合同已建立；业务代码、权限配置、初始化及运行验收均未实施。

适用范围：管理端列表、查询型报表、目录页面中的人员、组织、客户、供应商、结算主体、仓库及字典筛选。覆盖后端随列表返回候选、前端从查询结果反推候选两种实现。

实施入口：后续 agent 必须先读本文件、根目录规则和目标子项目 `AGENTS.md`，按第 8 章批次执行，第 9 章逐项登记。不得将本文的目标状态登记为已实现。

## 1. 产品规则与完成边界

1. 普通筛选器展示当前用户有权选择的对象；对象在当前业务列表中没有匹配记录，仍须允许搜索、选择。选择后列表正常返回空结果。
2. 销售人员候选由销售人员资格、人员查询 DataScope 和人员搜索条件决定；不得要求其拥有客户、合同、销售单、收付款或选品册。
3. 候选不得取自当前页、业务列表全量结果、业务汇总、已选择 ID 集合或导出结果。将当前页改成全量业务记录去重，不构成本合同要求的整改。
4. 候选接口与业务列表分别解析自身资源、动作的 RBAC 和 DataScope。候选可见不授予相关业务记录的读取权；选中 ID 不构成授权凭证。
5. 业务查询继续执行 `业务授权范围 ∩ 用户筛选条件`。不得在拆分候选时删除列表、总数、指标、详情或导出的授权校验。
6. 普通候选查询不接受消费页面的页码、业务关键词、业务状态、金额、日期等条件。候选自身的关键词、分页、状态，以及明确登记的组织等级联条件可以收窄候选。
7. 业务列表不再承担普通筛选候选的生产和传输。列表保留行、分页、授权元信息及原有业务指标；指标是否另拆接口不属于本次强制要求。
8. 改造不改变合同当前客户主负责人、销售单负责销售、采购负责人、实际经办人、任务处理人和历史归属的业务含义。
9. “无候选权限”“无候选范围”“候选搜索无结果”“候选加载失败”“业务筛选无结果”必须分别处理。读取失败不得伪装成请求成功后的空候选。
10. 本项目尚未上线，按批次直接切换目标接口、调用方及当前合同；不新增双读、旧字段回退或按错误自动调用账号管理接口的兼容路径。未迁移批次维持原行为，并明确登记未完成。

### 1.1 必须成立的示例

经理可查询销售甲、乙；甲有合同，乙无合同。销售候选必须同时包含甲、乙；选择乙后合同返回 `items=[]`、`total=0`。合同的翻页、状态、关键词变化不改变销售候选。经理失去乙的人员查询范围后，候选及已选项回显必须重新授权；合同本身仍按 `contract:list` 独立授权。

### 1.2 与既有合同的效力

1. 本文替代既有文档中“普通候选随列表返回”“从可见业务对象反推普通候选”“候选必须保证能筛出业务记录”的要求。替代范围仅限本次候选查询职责与接口拆分。
2. [组织与范围主合同](organization-data-scope-contract.md) 的授权组合、个人上限、历史参与、业务事实归属、事务和版本规则继续有效。候选与业务列表共用公共授权机制，各自使用其拥有对象的事实映射。
3. [S1](organization-data-scope-s1.md)、[S2](organization-data-scope-s2.md)、[S3](organization-data-scope-s3.md)、[S4](organization-data-scope-s4.md) 的既有完成登记不证明本文已完成。新增批次使用独立状态，不重写过去的验证结果。
4. `organization-owner-query-openapi.yaml`、`organization-data-scope-s2-openapi.yaml`、`organization-data-scope-s3-openapi.yaml`、`organization-data-scope-s4-openapi.yaml` 在对应代码切换批次同步更新。文档准备阶段不得先删除仍被实际接口返回的字段，制造虚假的已切换状态。
5. [列表搜索执行合同](../backend/docs/api/list-search.md) 中的业务搜索、排序与指标规则继续有效；普通候选规则以本文为准。

## 2. 分类与禁止混用

| 类型 | 数据源与授权 | 执行要求 |
| --- | --- | --- |
| 当前人员查询候选 | 独立人员目录资格及该目录的 DataScope | 允许零业务记录；保留查询需要的停用人员；不得复用分派资格 |
| 当前客户、供应商、结算主体、仓库候选 | 被选择对象的目录、资格及对应 DataScope | 优先复用符合本合同的对象查询；不得从消费业务记录去重 |
| 当前组织候选 | 组织目录及明确的组织查询范围 | 不从业务记录出现过的部门反推；业务页组织筛选仍按其业务归属解释 |
| 状态、类型、动作等固定字典 | 领域枚举、正式动作注册表或权威字典 | 使用完整合法集合，不从列表或统计中出现的值生成；不为常量新增 HTTP 请求 |
| 历史人员／组织归属候选 | 历史身份及冻结归属语义、当前查询授权 | 单独登记历史接口；不得用当前销售资格、当前部门覆盖历史身份和标签 |
| 命令、交接、改派、审批恢复候选 | 具体业务对象、动作、资格及岗位分离规则 | 保持专用资格接口；不得使用普通查询目录代替命令授权 |
| 当前对象内部选择 | 当前单据明细、选品册套餐档位等对象内事实 | 可保留局部选项；必须证明不承担跨对象目录筛选 |

历史候选允许从授权历史事实识别历史身份，是显式例外；历史候选接口必须独立于报表分页和当前结果集。拆分时必须登记其时间范围、快照名称及授权依据，不得把例外扩展到当前销售人员候选。

行展示名称与筛选候选必须分离：业务行已经授权时，按本域展示合同返回负责人／处理人名称；不得通过是否能读独立人员目录决定已授权业务行的基本可读性。页面不得依赖候选全集充当行名称字典。

## 3. 候选资源、资格与 DataScope

### 3.1 首批销售人员资源

目标接口固定为 `GET /admin/salespeople`，授权固定为 `sales_person:list`，拥有领域为 `erp-identity`。该资源为轻量业务人员查询，不是系统账号管理接口，不暴露密码、联系方式或账号管理操作。

| 事项 | 强制规则 |
| --- | --- |
| 销售资格 | 由身份域统一解析账号与正式销售资格关系；优先复用已存在的权威规则；不得按角色显示名、创建过单据或客户归属记录判断 |
| 资格登记 | B0 必须登记确切的资格来源、稳定角色／类别标识、停用与撤销含义及对应代码入口；不存在权威规则时，先补齐身份域规则再接入页面，不得各页面各自猜测 |
| 查询与分派 | 查询资格不能直接等同于 `sales_order:create` 等命令权限；具有筛选资格不等于可接收责任或可建单 |
| 停用与历史人员 | 账号停用不自动删除查询资格；离职、撤销销售角色与资格终止必须有独立约定。原系统可筛选的存量负责人不得因角色变化静默消失；需要保留的身份由资格历史／人员目录保存，不得再次依靠消费列表推导 |
| 本人范围 | 候选人员 ID 等于当前操作人，且符合该目录资格；没有业务记录也可出现 |
| 组织／团队范围 | 使用候选人员在授权时点的有效主属组织；复用显式组织、本人组织、管理组织及下级展开规则 |
| 公司范围 | 覆盖符合资格的人员；仍受个人上限、账号类型、软删除等边界约束 |
| 无组织 | 不视为属于任意组织；本人／公司范围是否允许按公共判定及资格规则执行，不补造部门 |
| 协作与历史参与 | 人员目录不从某张业务单的参与权扩张出人员读取权；未定义的范围类型必须在配置入口及解析边界拒绝 |
| 多角色与个人上限 | 复用同角色权限证明、角色正向范围并集、个人上限交集；不得自行把部门和人员条件拼成更宽的权限 |
| 空范围 | 无有效角色范围返回空候选及 `no_scope`；缺动作权限拒绝；不得回退公司范围 |

资格是目录对象集合的定义，DataScope 是操作人可读范围，两者必须分别建模。B0 未完成资格登记时，仅阻断该人员类别的接入，不得以随意加入全部管理员完成批次。

### 3.2 其他人员与对象资源

1. 采购、维护人、能力负责人、制单人、经办人、任务处理人分别登记候选语义。相同资格与相同授权对象可复用接口；不同资格或授权口径不得仅因为都返回 `id/name` 就合并。
2. 新增人员类别前先登记“接口、资源动作、拥有领域、资格来源、组织映射、状态规则、默认授权”。禁止每个页面新增一套人员目录，也禁止客户端传任意 `resource` 或 `permission` 绕过服务端固定注册。
3. 销售、采购混合筛选必须按页面字段语义登记允许的类别；服务端或前端组合均须保留每一类自身授权，不得以有销售查询权获取采购候选。
4. 客户、供应商、仓库已有列表接口符合轻量、分页、资格及授权要求时直接复用；若绑定管理权限、强制启用状态或携带沉重候选聚合，则在拥有领域提供独立轻量查询。不得为了选择器授予账号／主数据维护权限。
5. 结算主体按主体身份与有效业务资格查询，不能用客户 ID 或供应商 ID 代替。现有接口没有接入所需 DataScope 时，先完成资源注册与范围映射，再供合同页调用。
6. 人员目录范围与业务范围独立配置。禁止直接复制任意角色既有 `contract:list`、`sales_order:list` 的授权为人员目录授权。默认配置须明确发布；没有配置保持关闭。

### 3.3 权限及初始化交付

每个新增候选资源必须同时完成：

1. `erp-identity/src/service/access_control/consumers.rs` 的真实消费者登记、支持维度、范围类型限制及对象判定。
2. Handler 权限标注、管理端认证路由、权限生成物及角色权限管理界面的登记。
3. `service/iam/predefined_roles/`、`predefined_data_scopes.rs` 和相关开发种子的显式初始化规则；更新资源动作总目录及管理界面合法选项。
4. 发布一张角色默认授权表，逐项填写资源动作、Company／SelfOwned／组织目标模式及个人上限处理。不能以“所有能看合同的角色默认 Company”代替该表。
5. 重跑初始化不得恢复已撤销范围、覆盖人工配置或默默扩大现有账号权限。本文不授权执行真实库种子、清库或批量权限写入。

## 4. 查询接口合同

### 4.1 分页搜索

销售人员接口采用以下参数；其他新建普通目录接口复用相同基础语义，业务专用字段另行登记。

| 参数 | 规则 |
| --- | --- |
| `q` | 可选，去首尾空白，最多 200 字符；姓名、登录账号字面量搜索，不执行用户正则 |
| `page` / `page_size` | 从 1 开始；默认 1／20，页大小上限 100；过滤、授权先于分页 |
| `org_unit_ids` | 可选稳定 ID 集合，最多 100 个；与候选授权求交 |
| `include_descendants` | 只在指定组织时使用；由后端展开，不能扩大授权 |
| `scope_version` | 后续页携带第一页的候选授权版本；版本变化使用现有 `DATA_SCOPE_CHANGED` 语义 |

1. 按显示名、稳定 ID 排序；同名人员可通过登录账号和允许显示的部门区分。
2. 返回现有 `ApiResponse` 信封，`data` 含 `items/total/page/page_size`、`scope_version/policy_version/organization_version/as_of/empty_reason`。人员项只含 `id/name/account/status` 及允许显示的组织标签。
3. `total` 与 `items` 使用相同资格、搜索及授权条件。不得先分页全量账号后在内存过滤权限。
4. 同一请求的授权、资格与组织读取保持一致；复用调用方事务执行器，不在下层开启嵌套事务。
5. 候选版本独立于业务列表版本，不传合同版本给人员接口，不要求两个独立请求共享同一数据库快照。候选授权、组织及资格变化必须使相关候选结果失效；不能只依赖人员数量变化。
6. 候选版本不构成数据快照令牌；跨页发生目录数据变化时重新搜索，前端按 ID 去重，不把多页候选用作正式导出。业务导出继续执行原业务快照合同。
7. 所有普通候选 API 的未知参数、非法 ID、空 ID 片段和超限必须校验；不接受消费列表的 `owner_user_ids` 来预先缩小人员全集。

### 4.2 已选项回显

1. 销售人员回显固定为 `GET /admin/salespeople/selected?ids=...`，复用 `sales_person:list` 与同一资格、DataScope 判定，不要求账号详情管理权限。
2. `ids` 必填、去重、最多 100 个。回显不受搜索词、候选页码及当前搜索结果限制；组织搜索条件也不删除已选值。
3. 返回当前可读项及授权元信息；不存在和无权读取的 ID 均不返回名称及组织信息，不提供可用于区分存在性的额外字段。
4. 回显成功但未返回的已选 ID，前端标为“已选人员（当前不可用）”，允许移除。网络失败、403、版本变化分别处理，禁止统一 `catch → null`。
5. 不因候选搜索、分页、权限变化或回显失败自动清除业务 URL 中的筛选 ID。用户移除或重新应用条件后才能改变业务查询。
6. 业务列表独立校验输入并与自身授权求交；不得因不在候选当前页而忽略筛选，也不得把整个候选目录再次当成业务授权集合。

## 5. 前后端职责与改造边界

### 5.1 后端

1. 单域候选放拥有领域 Service／Repository；跨域只读组合放 `erp-read-models`。Handler 只适配协议，禁止直连数据库。业务域之间不新增直接依赖。
2. 复用现有 `DataScopeService`，不建立并行人员权限服务。Repository 接收明确授权条件，不按登录用户自行推断权限。
3. 合同删除 `ContractListView.owner_options`、`settlement_options` 及专用候选聚合。保留 `apply_list_filters`、当前客户主负责人事实、关键词搜索、排序、行名称、业务版本和授权指纹所需读取。
4. 其他资源采用相同步骤：沿字段追踪生产、序列化、前端消费、行显示、导出及测试，再删除仅服务候选的查询。共享类型仍有未迁移消费者时不得提前删除字段。
5. 不顺带拆分统计、不重写金额算法、不改变历史参与、不修改责任交接、不扩大当前迁移批次。
6. 人员或对象查询必须评估搜索、组织、资格与排序索引；禁止加载全公司账号或全部业务记录后在内存完成授权和分页。

### 5.2 前端

1. 共用 `features/entity-selectors` 的 API、Query hook 和选择器；组件只消费 hook。多选负责人增加远程搜索、加载更多及已选项回显能力。
2. 候选 Query key 包含对象类别、明确用途、候选搜索、组织级联和候选分页；不包含业务列表页码、合同状态、业务时间范围或业务搜索词。
3. 账号切换清空旧缓存；权限、组织、资格变更接入 `features/data-scope/cache.ts` 及 `QueryProvider` 现有失效机制。已选项缓存也必须失效。
4. 授权变化时不能用 `placeholderData` 或旧页继续展示已撤权人员信息。搜索条件变化时是否保留旧结果须与授权失效区别处理。
5. 列表与候选并行、独立加载。候选失败不抹掉已成功的业务列表；业务列表失败不抹掉有效候选。局部权限失败不得被错误地解释为整页业务权限丢失，需核对全局 Query 错误处理。
6. 保持稳定原生 DOM id、显式应用筛选、URL／前进后退／刷新一致性、已选 chip、清除与导出条件一致性。
7. 仅当用户明确选择组织级联时，将相应组织条件传给候选。未在控件上表达的条件不得偷偷影响候选集合。
8. 行名称放在业务行 DTO，或专用且经过业务授权的行展示补充读取中。筛选候选加载失败时，行负责人姓名不得全部退化为 ID。
9. 不固定取前 20／100 条充当完整目录；不让用户必须知道精确姓名才能找到后续页。已选对象跨页时保留名称，清空搜索不会丢失选中值。

## 6. 静态盘点与逐模块交付清单

盘点日期：2026-09-23。以下为源码入口，不代表运行验证。路径前缀 `B` 为 `backend/crates/`，`F` 为 `erp-client/features/`。后续执行者必须按当前工作树复核调用链；同一业务的多个 Tab 或多个返回字段不得只改其中一个。

### 6.1 已确认的普通候选耦合

| ID | 功能与字段 | 后端入口（B） | 前端入口（F） | 必须交付的替换 |
| --- | --- | --- | --- | --- |
| Q01 | 合同：负责人、结算主体 | `erp-contract/src/service/contract/scope.rs`；`repository/list_search.rs` | `contracts/api/list.ts`；`contracts/hooks/use-contracts-list.ts` | 销售人员目录＋结算主体目录；保留合同授权与归属查询 |
| Q02 | 客户目录：`owner_options`，含 all-authorized | `erp-customer/src/service/customer/scope.rs`、`query.rs` | `customers/api/directory.ts` | 销售人员目录；不按客户有无归属记录定义销售全集 |
| Q03 | 销售单：`owner_options` | `erp-read-models/src/sales_center/order/scope.rs`、`query.rs` | `sales-orders/api/sales-orders-list.ts` | 销售人员目录；创建人与负责销售字段继续分开 |
| Q04 | 采购单：`owner_options` | `erp-read-models/src/purchase_center/scope.rs`、`query.rs` | `purchase-orders/api/purchase-order-queries-api.ts` | 采购人员目录；保留当前采购责任语义 |
| Q05 | 选品册及销售方案：`owner_options` | `erp-processes/src/sales_selection/scope_checks.rs`、`mod.rs` | `sales-selection/api/books.ts`；复核方案调用方 | 销售人员目录；册／方案分别核销，不能因只看到册页面漏掉方案 API |
| Q06 | 商品：维护人、负责采购 | `erp-read-models/src/catalog_center/scope.rs`；`erp-catalog/src/dto/catalog/product_scope.rs` | `master-data/api/lists/products.ts`；`api/resource-queries.ts` | 维护人员及采购人员目录；当前维护人候选来自页行、采购候选来自已选 ID，均须替换 |
| Q07 | 供应商：维护人、能力负责人 | `erp-supplier/src/service/supplier/scope.rs` | `master-data/api/lists/supplier.ts`；`api/resource-queries.ts` | 按字段资格接独立目录，不把两个负责人当成同一岗位 |
| Q08 | 供应商供给：维护人、负责采购 | `erp-read-models/src/supplier_center/offering/scope.rs`、`dto.rs` | `supplier-offerings/pages/supplier-offerings-page.tsx`；`types.ts` | 当前页／已选 ID 候选替换；表格展示也在消费 owner options，须解耦 |
| Q09 | 供应商订单：跟进人、处理人 | `erp-processes/src/supply_execution/list.rs`；`erp-supply/src/dto/supplier_fulfillment_scope.rs` | `supplier-orders/api/list.ts` | 跟进人／处理人查询目录；当前代码用候选补行姓名，删除前先补业务行字段 |
| Q10 | 供应商结算：owner/operator/handler | `erp-supply/src/service/supplier_settlement/query.rs`；`dto/supplier_settlement/query.rs` | `supplier-settlements/api/settlements-list.ts`、`settlements-wire.ts` | 三类人员候选目前从页内 prepared_by／difference_handler_user_id／reviewed_by 生成；先核对实际字段语义再接独立目录 |
| Q11 | 客户应收、回款、开票申请 | `erp-read-models/src/finance/funds_scope/{receivable,receipt,request,rows}.rs` | `customer-receivables/api/scoped-view.ts`；`invoice-requests/scoped-view.ts` | 销售人员目录；保留资金份额授权与金额口径 |
| Q12 | 供应商应付、付款、进项分配 | `erp-read-models/src/finance/funds_scope/{payable,payment,allocation,rows}.rs` | `supplier-payables/api/scoped-view.ts`；沿各 handler 查其他消费方 | 采购人员目录；不得因替换候选放宽关联采购单授权 |
| Q13 | 发票：销售／采购混合负责人 | `erp-read-models/src/finance/funds_scope/invoice.rs` | 沿 `funds-scope.ts` 与客户／供应商往来发票 Tab 复核 | 按发票方向登记目录类别，不能无条件并入全部销售及采购 |
| Q14 | 客户质量现任视图：负责人、组织 | `erp-read-models/src/customer_quality/current.rs`、`dto.rs` | `customer-quality/` 内查询、类型与筛选组件 | 销售人员／组织目录；当前来源为授权客户事实，仍不满足零业务候选要求 |
| Q15 | 履约队列：仓库 | `erp-read-models/src/workbench/fulfillment_queue.rs` 及其 repository_page 来源 | `fulfillment-operations/api/queue.ts` | 核对仓库集合是否由任务反推；普通仓库筛选接仓库目录，任务执行资格保持原规则 |

Q10 的 `handler_options` 当前取 `reviewed_by`，不能仅凭字段名替换为“当前待办处理人”。若既有参数、文案、字段事实不一致，先在本批登记中锁定原合同语义；实质业务纠错另列，不随候选拆分默改。

### 6.2 扩展盘点、显式例外与公共收尾

| ID | 入口／对象 | 分类与执行要求 |
| --- | --- | --- |
| Q16 | `B/erp-read-models/src/customer_quality/history.rs`、`finance/actual_profit_loss/{projection,attribution,dto}.rs` | 历史人员／组织例外；独立历史查询，保留冻结归属，不接当前人员目录 |
| Q17 | `F/access-audit/pages/hooks/use-access-audit-page.ts` 的 `actionOptions` | 当前从事件结果生成动作候选；改用审计动作权威注册表，允许选出零事件 |
| Q18 | `F/actual-profit-loss/hooks/use-profit-loss-filter-presentation.ts` 的 `costTypeOptions` | 当前取 `costComposition`；改用成本类型权威字典。福利场景已来自常量，复核后保留 |
| Q19 | `F/entity-selectors/api/{customers,parties,suppliers,warehouses,contracts}.ts`；`hooks/queries.ts` | 已有独立查询不自动算合格：核对 DataScope、用途、固定第一页、强制 active、回显吞错与缓存。业务录入和查询筛选不能共用隐含资格 |
| Q20 | `F/master-data/api/lists/products.ts` 的 `fetchProductFilterOptions`；`F/inventory/api/list.ts` 的仓库读取 | 已独立读取的路径，复核分页、权限与错误隔离；不为了形式统一重复造接口 |
| Q21 | `B/erp-workflow/src/service/work_item/finance_responsibility.rs`；路由 reviewer-options、warehouse-fulfillment-handler-options、recovery-options；`F/procurement-responsibilities/` | 逐一判断查询或命令用途；命令／分派候选保留专用规则，禁止批量替换。普通查询消费方另行拆出 |
| Q22 | `F/sales-selection/pages/public-selection-page.tsx` 的套餐档位 | 当前对象内部选择例外；不得把公开选品 token 流程接入管理端人员目录 |
| Q23 | `B/application-core/src/query_ids.rs` 的 `FilteredPage`；`erp-client/lib/collect-pages.ts`、`erp-client/lib/funds-scope.ts` | 所有调用方迁移后移除候选响应与跨页合并；保留分页、版本、完整导出检查及 ownership_basis |

### 6.3 防遗漏搜索

每批开始及总收尾在仓库根执行以下静态搜索，并记录新增入口；命中结果必须分类，禁止机械删除所有 `options`：

```bash
rg -n 'owner_options|settlement_options|procurement_owner_options|capability_owner_options|handler_options|operator_options|org_options|warehouse_options|attribution_user_options|attribution_org_options' backend/crates erp-client/features erp-client/lib docs
rg -n 'Options\s*=|options\s*:|new Set\(' erp-client/features --glob '!*.test.*'
rg -n 'filter_options|current_owner_ids|current_options|history_options' backend/crates
rg -n 'ownerOptions|handlerOptions|operatorOptions|settlementOptions|warehouseOptions' erp-client/features erp-client/lib
```

静态命中只用于盘点，不可替代行为测试。搜索以当前源码为准，不修改归档目录或历史证据目录。

## 7. 必须保持的验收行为

| 编号 | 输入／场景 | 必须结果 |
| --- | --- | --- |
| A01 | 范围内合格销售无任何业务记录 | 可搜索、分页找到及选中；业务列表为空 |
| A02 | 范围外销售有业务记录 | 人员目录及回显不泄露姓名、部门 |
| A03 | 候选人员可见，关联业务部分可见 | 列表、总数、指标、导出仅返回业务授权范围 |
| A04 | 修改业务页码、状态、关键词、期间 | 人员候选请求条件和候选范围不变 |
| A05 | 独立候选组织级联 | 与候选授权求交；清除组织恢复其授权全集，不补 Company |
| A06 | 同名、多页、已选不在当前页 | ID 区分、分页可达、回显正确、不误判不可用 |
| A07 | 停用、调岗、资格终止、历史身份 | 按登记的查询资格保留或标记；命令资格不被放宽；历史归属不改写 |
| A08 | 缺权限、缺范围、非法参数、越界 ID | 拒绝／空范围按合同区分；筛选不被忽略；业务范围不扩大 |
| A09 | 权限、组织、资格变化及切换账号 | 候选与回显缓存失效；旧私有标签不继续展示；URL 不被静默清除 |
| A10 | 候选失败、业务成功；或业务失败、候选成功 | 独立展示及重试；不抹掉另一查询数据 |
| A11 | 手工 URL、刷新、前进后退、清除、导出 | 查询条件一致；既有业务版本及完整导出校验保持 |
| A12 | 候选无法读取或失败，但业务行已授权 | 行的负责人／处理人名称仍按业务展示合同可读 |
| A13 | 普通候选用于交接、改派、审批等命令 | 命令仍独立执行专用资格、对象范围和岗位分离校验 |
| A14 | 初始化重跑、已有人工配置或撤销记录 | 不恢复已撤销授权，不自动扩大角色或个人范围 |
| A15 | 移除共享 options 字段与合并逻辑 | 所有实际调用方已切换；分页、统计、导出、行展示无依赖残留 |
| A16 | 审计动作／成本类型没有对应业务记录 | 固定合法字典值仍可选择，业务结果为空 |

后端只新增／修改内联库单元测试，使用真实生产规则与必要替身；禁止源码字符串断言代替行为验证。当前前端规则禁止新增／修改单元测试，除非用户在实施任务中明确授权；使用现有检查及允许的隔离／模拟验证登记前端证据，不为保持旧响应测试通过而恢复旧字段。

不新增、修改、运行后端集成测试、真实 Mongo／外部服务验收例程；不调用会清库的 E2E 编排。未获允许或受规则限制的运行验收必须列为未验证，不能用静态检查替代。

## 8. 执行批次与完成门槛

| 批次 | 依赖 | 交付范围 | 退出条件 | 当前状态 |
| --- | --- | --- | --- | --- |
| B0 基线与资格登记 | 无 | 复核 Q01—Q23；登记各候选类别资格、接口、资源动作、角色默认授权及状态规则 | 第 9 章登记表完整；普通／历史／命令候选分类无混用；缺口有明确前置任务 | 未开始 |
| B1 公共销售目录与选择器 | B0 销售登记 | 身份域 salespeople、DataScope／权限／种子、回显、多选远程选择器、缓存失效 | A01、A02、A05—A10、A14 本地证据；接口可独立接入，不依赖合同集合 | 未开始 |
| B2 合同完整样例 | B1、结算主体目录前置 | Q01 负责人和结算主体；前后端与相关 OpenAPI 同批切换 | A01—A12 适用项完成；候选查询不随合同查询变化；行显示完整 | 未开始 |
| B3 销售链路 | B2 | Q02、Q03、Q05、Q14；按功能逐项交付 | 每个接口／页面／Tab 独立核销，当前人员／组织不依赖业务记录 | 未开始 |
| B4 采购与主数据 | B0 对应类别登记、B1 组件 | Q04、Q06—Q08、Q19—Q20 对应入口 | 资格区别清楚；前后端不再生产当前页或已选 ID 伪全集 | 未开始 |
| B5 执行与财务 | B3／B4 所需目录 | Q09—Q13、Q15；复核 Q21 | 三类人员、行名称、任务资格、资金份额均保留；不能仅改一个资金 Tab | 未开始 |
| B6 历史与字典 | B0 分类 | Q16—Q18、Q22 分类结论 | 历史接口及例外登记完成；普通字典不由结果集限制 | 未开始 |
| B7 公共收尾 | B2—B6 | Q23、剩余 Q19—Q21、全仓搜索与文档核对 | 所有普通候选消费者切换；历史／命令／对象内例外均有依据；无旧字段回退 | 未开始 |

1. 每次实施以用户指定批次为界；未指定时从最早未完成的前置批次开始。不得跳过销售资格和默认授权登记直接改 UI。
2. 每个功能必须纵向完成后端、前端、权限配置代码、文档及适用检查。基础接口可以先交付，消费页面不得进入接口未完成的半切换状态。
3. 不要求一次修改所有页面。不能因为其他批次未完成而让已迁移页面保留旧列表 options 回退。
4. 状态使用“未开始／执行中／本地检查通过／已完成／已验收”。“已完成”要求该批代码与文档交付及本地门槛核销；“已验收”另需真实账号、数据库与业务证据。文档完成不等于任何代码批次完成。
5. 工作树已有修改必须保留；改动重叠时先阅读差异并在交付记录列出本批拥有的改动。不得把他人筛选样式调整回滚掉，不得未经要求提交 Git。

### 8.1 本地检查

1. 文档批次只执行 `git diff --check`、新增文档格式及链接检查，不编译业务代码。
2. 后端按 `backend/AGENTS.md` 执行受影响 crate 的 fmt、check、库单元测试和 clippy；公开签名变更追加 workspace check；数据访问和跨 crate 变更执行领域边界门禁；权限标注变更执行权限漂移门禁并同步生成物。
3. 前端按 `erp-client/AGENTS.md` 执行相应 lint、格式和类型检查；不得擅自改既有单元测试。旧测试受接口变化影响时如实登记，不能宣称整套测试通过。
4. 提交阶段的全量门禁仅在需要提交时按仓库规则执行；运行与验收限制始终有效。静态、库单元、隔离／模拟浏览器、真实数据库、真实业务验收分别列出。

## 9. 后续 agent 的交付记录格式

每批在本章追加一份记录，采用以下固定字段。只记录最终执行合同、状态与证据，不写讨论过程或尝试复盘。

```text
批次／功能 ID：
状态与日期：
本批拥有的文件／已有他人改动边界：
候选分类：普通目录／历史／命令／固定字典／对象内
原候选来源及生产—消费调用链：
目标接口／拥有领域／资源动作：
资格权威来源、稳定标识、停用／撤销／历史身份规则：
对象组织映射与合法 DataScope 类型：
角色默认授权及初始化幂等规则：
输入／响应／回显／版本／索引：
前端 Query key、授权失效、行名称与错误隔离：
已删除的旧字段／仅服务候选的查询／回退逻辑：
保留的业务授权／事实查询／命令资格及理由：
同步的合同／OpenAPI／权限生成物：
验收编号、命令、结果与证据位置：
未验证项及真实验收跟踪：
前置缺口／下一可执行批次：
```

本合同建立时的记录：仅完成静态盘点和执行规则；B0—B7 均未开始。未运行程序、单元测试、真实数据库、浏览器或业务验收。

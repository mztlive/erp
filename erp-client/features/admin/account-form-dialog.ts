/**
 * 新建 / 编辑账号对话框稳定入口。
 * 实现见 components/accounts/account-form-dialog；本文件只做再导出。
 */

export { AccountFormDialog } from "./components/accounts/account-form-dialog"
export type { AccountDraft } from "./components/accounts/account-form-dialog"

//! D07 敏感值指纹密钥（数据模型 §4.5.5）。
//!
//! 联系人手机号、履约地址、银行账号的查询指纹使用带密钥 HMAC-SHA256。
//! 指纹密钥属于系统级密钥体系，P0 未提供配置注入；本阶段使用固定开发密钥
//! 并保持与实体层单元测试一致（`b"test-fingerprint-key"` 同源）。**地基修订
//! 候选**：密钥体系下沉到 config/AppState 后由 Service 注入，换密钥会使既有
//! 指纹全部失效，需要一次性重算。

/// 敏感值查询指纹密钥（开发期固定值；换密钥后旧指纹全部失效）。
pub const FINGERPRINT_KEY: &[u8] = b"erp-party-fingerprint-key-p0";

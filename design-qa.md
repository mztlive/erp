# 销售单详情布局验收

- 验收范围：已提交销售单详情，采用用户选定的第 1 张布局。
- 设计基准：`/Users/huangjiajiang/.codex/generated_images/01a07fdd-c99c-7151-a75b-2930c06f891f/exec-360de34a-8125-4ff6-9e99-058604fff256.png`。
- 页面：`http://localhost:3000/sales/orders/3e881ada0f9a4d4aba1d76865340e913?section=overview`。
- 数据状态：实物与服务、审批中、1 行明细、成交金额 1288.00 元、无已生效版本。
- 桌面对照：设计图 1487 × 1058 像素；浏览器视口 1727 × 1058 CSS px，去除左侧 240px 应用导航后截取 1487 × 1058 像素。截图密度 1，无缩放。
- 最终截图：`.local/sales-detail-qa/desktop-final.png`。
- 窄屏证据：`.local/sales-detail-qa/mobile.png`、`.local/sales-detail-qa/tablet.png`，分别检查 390 × 844、768 × 1024 CSS px。

## 验收规则与结果

| 检查项 | 必须满足 | 结果 |
| --- | --- | --- |
| 内容主次 | 左侧依次呈现明细、采购进度、交易约定；右侧独立呈现金额、流程和责任人 | 通过 |
| 字体 | 客户标题、成交金额、分区标题、辅助标签具有明确字号和字重差异 | 通过；使用项目现有字体与字号体系 |
| 比例与间距 | 桌面主栏约七成，摘要侧栏约三成；窄屏退为单列 | 通过；最终侧栏 400px，内容区无横向溢出 |
| 色彩 | 白色主栏、浅灰侧栏；当前流程节点用状态色突出 | 通过；复用现有主题变量 |
| 图片与图标 | 不引入商品占位图片；流程节点使用现有图标库 | 通过；选定设计无独立位图资产 |
| 内容准确性 | 金额、状态、负责人、版本和禁用操作使用真实业务数据 | 通过；不复制设计图中缺少业务依据的日期和数量合计 |
| 页面操作 | 侧栏查看审批进入审批页签，概览可返回 | 通过；浏览器验证 |
| 控制台 | 最终刷新后无新增错误 | 通过；状态组件仅接收标签和颜色，禁止透传业务字段 |

## 对照检查记录

| 检查 | 证据 | 修正及复核 |
| --- | --- | --- |
| 首轮 P2：字号层次偏弱、侧栏偏窄 | `desktop.png` 与设计基准同次对照 | 加大客户标题、金额和分区标题，侧栏加宽；`desktop-final.png` 复核通过 |
| 最终对照 | 设计基准与 `desktop-final.png` 同次展示比较 | 无待修复 P0/P1/P2；完整截图中标题、表格与金额可读，无需局部放大 |

## 实现边界

- 保留系统导航、真实权限控制、回款与开票状态；空内部说明不占位。
- 不跨单位累加数量，不补造节点完成日期；采购统计沿用原接口口径。
- P3：字体、轻边框与控件尺寸沿用项目组件，非逐像素复制生成图。
- 相关测试：3 个文件、6 个测试通过；修改文件 lint 与 diff 检查通过。
- 最后全项目类型检查未通过：`features/workspace/components/workspace-filter-bar.tsx` 的 `ListWorkspaceFilterBar`、`ListSearchField` 未定义，位于本任务修改范围之外。
- 未验证卡券真实页面、审批提交、撤回或改单写入；本结果不作为这些业务操作的验收凭证。

final result: passed

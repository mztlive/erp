# 销售单与合同浮动筛选验收

final result: blocked

## 适用范围

本次仅在销售单、合同列表启用方案 2。其余页面保留现有筛选展示方式。

设计依据：`/Users/huangjiajiang/.codex/generated_images/01a0cbc5-23ff-7be0-a431-6f504248131d/exec-e95ebf7b-8822-41e4-948a-851f7a73431e.png`。

源图尺寸为 1024 × 1536 像素，包含两个页面的方案展示，不是单一浏览器视口。实际验收必须分别比对销售单和合同内容区域，不得直接对整张展示图作逐像素结论。

## 实施约束

1. 销售单常驻关键词、负责销售、商业状态、创建日期和业务性质；其他条件放入分组浮动面板。
2. 合同常驻关键词、结算主体、当前跟进负责人；组织条件放入紧凑浮动面板。
3. 面板必须锚定“更多筛选”，不得推移表格；正文可滚动，页脚操作必须可见。
4. “查询”和“应用筛选”提交全部草稿并回到第一页；取消、关闭、Esc、外点撤销面板草稿，保留外部条件草稿。点击外部“查询”不得先触发取消而丢失草稿。
5. “重置”仅清除面板草稿；“清除全部”清除所有条件；已生效条件必须支持单独移除。
6. 组织按名称选择，不展示内部 ID。组织候选沿用现有 `org_unit:list` 权限；无权限时不得请求组织列表，失效已选项必须保留并允许移除。
7. 导出继续使用已生效条件，不使用未提交草稿。
8. 常驻控件必须始终显示字段名。商业状态、结算主体、负责销售、跟进负责人及创建日期在选中后不得仅显示值。
9. 负责人常驻控件使用固定 `h-control` 高度和选择摘要；多选不得用标签换行撑高查询栏。
10. 已选条件必须在业务性质下方独立成行；结果数量与列设置保留在下一层表格工具栏。没有已选条件时收起该行。
11. 组织选择与“包含下级”必须保持同一行：左侧选择框自适应，右侧复选框固定宽度并与首行控件垂直居中；长组织名称截断，不得挤压复选框。销售单与合同共用此布局，待浏览器复验。

## 标注截图修正验收

修正依据：`/var/folders/hp/2pcnc0sd05zdp3blph8r6yjr0000gn/T/codex-clipboard-SOjhIT.png`。该图与方案源图已同时查阅；它仅证明修正前的问题，不代表修正后验收。

| 严重级别 | 截图证据 | 修正要求及当前状态 |
| --- | --- | --- |
| P2 | 商业状态选中后只显示“审核中” | 字段名与输入值分开渲染；已改代码，待新截图复验 |
| P2 | 负责销售多选输入低于相邻控件 | 改用固定高度摘要按钮，搜索与多选在弹层内完成；已改代码，待新截图复验 |
| P2 | 已生效标签与结果数量混排 | 已选条件独立成行，保留表格工具栏职责；已改代码，待新截图复验 |

本轮检查：8 个修改文件 Oxlint 与 Oxfmt 通过；4 个既有回归文件、21 个用例通过；TypeScript 仍报告下表列出的 3 处非本次修改错误，未报告新增类型错误。

## 已执行检查

| 检查 | 结果与边界 |
| --- | --- |
| 修改文件 Oxlint | 通过，10 个 TS/TSX 文件 |
| 修改文件 Oxfmt | 通过，10 个 TS/TSX 文件 |
| 既有回归测试 | 5 个文件、24 个用例通过；覆盖共享查询栏、列表框架、负责人选择、合同列表及导出 API；未新增或修改测试 |
| TypeScript 全量检查 | 未通过；错误位于本次未修改的 integration-errors 和 master-data 文件 |
| Feature 循环依赖检查 | 未通过；customer-receivables 与 invoice-requests 存在循环，本次未修改这两个模块 |
| 金额规范检查 | 未通过；customer-receivables/lib/scoped-amount.test.ts 第 80、81 行使用 Number，本次未修改 |
| 数据写入与 E2E | 未执行 |

## 浏览器验收门禁

实现截图路径：未生成。浏览器连接 `iab`、`chrome` 均返回 `Browser is not available`；原生 Chrome 入口返回 `Invalid app`。

验收视口：桌面 1440 × 1024、窄窗口 768 × 1024；CSS 尺寸与截图像素密度必须记录。实现截图尺寸、设备像素比、密度归一化：待浏览器可用后记录。

必须逐项执行：

- 分别捕获两个页面的收起态、展开态、编辑态、应用后状态；核对常驻控件布局与表格位置。
- 将源图对应页面区域与实现截图置于同一比对输入；另行比对筛选栏、面板正文及固定页脚。
- 检查中文字体、字号、字重、行高、换行及截断；当前仅确认使用既有字体与组件。
- 检查间距、对齐、宽高、圆角、阴影和窄屏边界；当前仅完成源码检查，不作视觉通过结论。
- 检查黑白灰令牌、文字对比度和状态色；不得把方案图示例数据复制到业务页面。
- 图标复用现有 Lucide；本设计无须新增位图资产。
- 检查文案、组织名称、权限说明、已生效计数和待查询提示。
- 验证嵌套下拉选择不会误关父面板；验证应用、重置、取消、Esc、外点、外部查询按钮及焦点返回。
- 验证组织无权限、无范围、加载失败、失效已选项和重试；验证筛空、清除、刷新与历史导航。
- 检查浏览器控制台错误、列设置唯一性、导出与查询条件一致性。

## 待关闭项

- 浏览器视觉与交互验收尚未执行；不得据静态检查或既有回归测试认定新面板验收通过。
- 全量检查中的既有错误须单独处理，当前不得报告前端全量门禁通过。

视觉比对记录：已通过用户标注截图识别上表三项修正前偏差；修正后尚无浏览器截图，不能关闭对应验收项。取得新截图并完成以上门禁后，方可将 final result 改为 passed。
